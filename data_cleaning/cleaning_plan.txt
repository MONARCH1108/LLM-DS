Step 1: Handle Missing Values
Affected columns: Age, Cabin, Embarked
Condition: Skip if columns are to be dropped entirely or if missingness itself is a feature to be encoded.
Action: Impute missing values using appropriate strategies (e.g., median/mean for 'Age', mode for 'Embarked', 'Unknown' or feature engineering for 'Cabin').
Severity: High

Step 2: Address Outliers in Numerical Features
Affected columns: Age, SibSp, Parch, Fare
Condition: Skip if outliers represent valid data points critical for analysis or if robust models are chosen that are insensitive to outliers.
Action: Identify and treat outliers using methods like capping, transformation (e.g., log transformation if also addressing skewness), or removal of extreme observations if confirmed as errors.
Severity: High (Parch, Fare), Medium (Age, SibSp)

Step 3: Manage High Cardinality Categorical Features
Affected columns: Name, Ticket, Cabin
Condition: Skip if these features are not required for downstream analysis/modeling or if a simplified representation (e.g., dropping) is acceptable.
Action: Apply feature engineering (e.g., extract titles from 'Name', categorize 'Ticket' patterns, extract deck from 'Cabin') or suitable encoding techniques (e.g., target encoding, frequency encoding, or grouping rare categories).
Severity: Medium

Step 4: Address Data Skewness
Affected columns: SibSp, Parch, Fare
Condition: Skip if downstream models are robust to skewed data distributions (e.g., tree-based models) or if domain knowledge advises against transformation.
Action: Apply appropriate mathematical transformations (e.g., log transformation, square root transformation) to reduce skewness and normalize feature distributions.
Severity: Medium

Overall cleaning complexity: High.