Step 1: Handle Missing Values
Affected columns: `track_name`, `album_name`
Condition: Skip if these columns are not essential for downstream analysis or if the percentage of missing values is extremely low and dropping rows is acceptable.
Action: Impute missing values for `track_name` and `album_name` with a placeholder string (e.g., 'Unknown Track', 'Unknown Album').
Severity: Medium

Step 2: Address Outliers
Affected columns: `popularity`, `stream_count`, `explicit`
Condition: Skip if the outliers are confirmed to be valid data points representing true rare events that should be preserved for specific analytical goals.
Action: For `stream_count` and `popularity`, investigate distribution and cap/floor values or apply robust scaling. For `explicit`, ensure values are strictly binary (0 or 1); recode invalid values to mode or null.
Severity: High

Step 3: Handle Skewed Distributions
Affected columns: `stream_count`
Condition: Skip if downstream models or analyses are not sensitive to feature distribution (e.g., tree-based models) or if the business context dictates preserving the original scale.
Action: Apply appropriate mathematical transformations (e.g., log, square root, Box-Cox) to normalize the `stream_count` distribution.
Severity: Medium

Step 4: Address High Cardinality Categorical Features
Affected columns: `track_id`, `track_name`, `artist_name`, `album_name`, `release_date`
Condition: Skip if these columns are used solely as identifiers and not as features for modeling, or if the chosen modeling technique can handle high cardinality directly.
Action: For `track_id`, verify its role as a unique identifier. For `track_name`, `artist_name`, `album_name`, and `release_date`, consider grouping rare categories, using frequency encoding, or other advanced encoding techniques suitable for high cardinality when used as features.
Severity: High

Overall cleaning complexity: high